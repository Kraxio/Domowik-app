# Źródłowe zrzuty (PNG) — na stronie używamy zoptymalizowanych .webp
screens/*.png

# Materiały źródłowe/reklamowe (nie część witryny)
screens/reklama/

# Robocze źródła screenów DE (PNG + PDF) — na stronę idą tylko gotowe de-*.webp
screens/ScreenshotsDE/
screens/ScreenshotsFR/
